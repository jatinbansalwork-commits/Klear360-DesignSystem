export {
  subtleFontSizes,
  normalAmountSizes,
  currencyHardcodedSizes,
  amountLineHeights,
} from './amountTokens';

/** @typedef {import('./amountTokens').AmountTypeProps} AmountTypeProps */
/** @typedef {import('./amountTokens').AmountBodyProps} AmountBodyProps */
/** @typedef {import('./amountTokens').AmountDisplayProps} AmountDisplayProps */
/** @typedef {import('./amountTokens').AmountHeadingProps} AmountHeadingProps */
/** @typedef {import('./slots').AmountSlot} AmountSlot */
