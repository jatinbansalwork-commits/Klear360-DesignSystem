import { Meta } from '@storybook/addon-docs/blocks';
import { List, ListItem, ListItemText, Heading, Code } from '../../src/components';

<Meta title="Guides/Klear360 MCP" />

<Heading size="xlarge">Klear360 MCP</Heading>
Klear360 MCP is a [Model Context Protocol (MCP)](https://modelcontextprotocol.io/introduction) server that
implements Klear's Design Guidelines and allows you to build Web Interfaces with AI using Klear360 Design
System.

<br />

<Heading size="large">Available Tools</Heading>

export const toolsData = [
  {
    name: 'hi_klear360',
    description:
      'Provides a welcome message and overview of Klear360 MCP capabilities when user greets with "hi klear360", "hey klear360", etc.',
  },
  {
    name: 'create_new_klear360_project',
    description:
      'Creates a new project using Klear360 with Vite, React, and TypeScript setup. Should only be called when creating a new project from scratch.',
  },
  {
    name: 'create_klear360_skill',
    description:
      'Creates the UI code guidelines skill for AI-assisted frontend code generation. Scaffolds the skill in .agents/skills/ui-code-guidelines and creates a symlink in .claude/skills for Claude Code support. Should be called before fetching component docs when the skill does not exist.',
  },
  {
    name: 'get_klear360_component_docs',
    description:
      'Fetches the Klear360 Design System documentation for specific components. Useful when adding or modifying components in your project.',
  },
  {
    name: 'get_klear360_pattern_docs',
    description:
      'Fetches the Klear360 Design System pattern documentation. Use this to get information about design patterns, best practices, and implementation guidelines.',
  },
  {
    name: 'get_klear360_general_docs',
    description:
      'Fetches general Klear360 Design System documentation. Use this to get information about setup, installation, theming, tokens, and general guidelines.',
  },
  {
    name: 'get_klear360_changelog',
    description:
      'Fetches the Klear360 changelog to help with upgrades and release notes. Supports a specific version or a range of versions.',
  },
  {
    name: 'get_figma_to_code',
    description:
      "Converts Figma designs into Klear360 Design System code. Provide a Figma design URL to generate the corresponding React components using Klear360's component library. (Klear employees only)",
  },
];

<table style={{ width: '100%', borderCollapse: 'collapse', marginBottom: '1.5rem' }}>
  <thead>
    <tr style={{ backgroundColor: '#f6f6f9', borderBottom: '2px solid #e8e8ed' }}>
      <th style={{ padding: '0.75rem 1rem', textAlign: 'left', fontWeight: '600' }}>Tool Name</th>
      <th style={{ padding: '0.75rem 1rem', textAlign: 'left', fontWeight: '600' }}>Description</th>
    </tr>
  </thead>

  <tbody>
    {toolsData.map((tool, index) => (
          <tr key={index} style={{ borderBottom: '1px solid #e8e8ed' }}>
            <td style={{ padding: '0.75rem 1rem' }}>
              <Code size="small">{tool.name}</Code>
            </td>
            <td style={{ padding: '0.75rem 1rem' }}>{tool.description}</td>
          </tr>
        ))}
  </tbody>
</table>

<br />

<Heading size="large">Pre-requisite</Heading>

<List marginBottom="spacing.5">
  <ListItem>
    <ListItemText>You must be running Node version >=18.18.0</ListItemText>
  </ListItem>
</List>

<br />

<Heading size="large">Integrations</Heading>

<br />

<Heading size="medium">Cursor or VS Code</Heading>

Create or update your `mcp.json` file with:

```json
{
  "mcpServers": {
    "klear360-mcp": {
      "command": "npx",
      "args": ["-y", "@klear/klear360-mcp@latest"]
    }
  }
}
```

<Heading size="medium">Claude Desktop</Heading>

Add the following to `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "klear360-mcp": {
      "command": "npx",
      "args": ["-y", "@klear/klear360-mcp@latest"]
    }
  }
}
```

> * Learn about how to configure MCP servers in [Claude Desktop](https://modelcontextprotocol.io/quickstart/user)
> * If you're using `nvm`, you might want to [follow these steps](https://github.com/modelcontextprotocol/servers/issues/64) instead of `npx`
> * Learn how to install [Claude Desktop](https://claude.ai/download)

<br />

<Heading size="medium">How to use</Heading>

* Follow [Integrations Guide](#integrations) to configure MCP servers in Cursor
* Open Cursor, Click "Open Project" and select an empty folder
* Press CMD + I (or CTRL + I) to open Cursor's chat
* Type "Hi klear360" and get started

<Heading size="medium">Usage Example</Heading>

```
Can you create a signup form with best UX practices using Klear360?
```

The AI agents will use the MCP server to retrieve components and generate appropriate code.
