/* eslint-disable @typescript-eslint/no-unused-vars */
import type { TableEditableSearchCellProps } from './types';
import { Text } from '~components/Typography';

const TableEditableSearchCell = (props: TableEditableSearchCellProps): React.ReactElement => {
  return <Text>Table Component is not available for Native mobile apps.</Text>;
};

export { TableEditableSearchCell };
